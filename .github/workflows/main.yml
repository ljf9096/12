name: main.py1

on:
   schedule:
    # 每周三北京时间凌晨1点
    # UTC 时间 = 北京时间 - 1小时 = 周二 17:00
    # 格式: 分 时 日 月 周 (0-6, 0=周日, 3=周二)
    - cron: '0 17 * * 2'
  workflow_dispatch: # 允许手动触发

jobs:
  run_daily_job:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout 仓库（使用内置 GITHUB_TOKEN）
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # 2️⃣ 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # 3️⃣ 安装依赖
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install opencc-python-reimplemented

      # 4️⃣ 运行 Python 脚本
      - name: Run Python script
        run: python main.py

      # 5️⃣ 提交更改（无改动不会报错）
      - name: Commit changes
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "github_actions[bot]"
          git add live.txt live.m3u live_lite.txt live_lite.m3u others.txt
          git commit -m ":tada: Daily AutoUpdate $(date +'%Y%m%d')" || echo "No changes to commit"

      # 6️⃣ 推送到远程仓库
      - name: Push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: git push origin main
